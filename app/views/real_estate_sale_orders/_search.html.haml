#searchbox
  = form_tag real_estate_sale_orders_path, :method => 'get' do
    = text_field_tag :search, params[:search], :class => "input-text", :placeholder => 'Search Real_estate_sale_orders'
    = image_submit_tag "admin/blank.png", :class => "image-submit"

