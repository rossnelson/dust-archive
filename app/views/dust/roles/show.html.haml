- title "Role"
%p
  %strong Name:
  = h @role.name
%p
  = link_to "Edit", edit_dust_role_path(@role)
  |
  \#{link_to "Destroy", @role, :confirm => 'Are you sure?', :method => :delete} |
  \#{link_to "View All", dust_roles_path}
