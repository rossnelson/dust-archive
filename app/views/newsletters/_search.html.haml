#searchbox
  = form_tag newsletters_path, :method => 'get' do
    = text_field_tag :search, params[:search], :class => "input-text", :placeholder => 'Search Newsletters'
    = image_submit_tag "admin/blank.png", :class => "image-submit"

